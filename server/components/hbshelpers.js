
var helper = {
    
    calculation:(value)=>{
            return value + 7;
        },

        shortText:(text, n)=>{
            return text.substring(0,n-1);
        } 
        
}
module.exports = helper;